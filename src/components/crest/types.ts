export interface CrestConfig {
  shape: string;
  division: string;
  primaryColor: string;
  secondaryColor: string;
  borderColor: string;
  icon: string | null;
  iconColor: string;
  showText: boolean;
  textColor: string;
}
